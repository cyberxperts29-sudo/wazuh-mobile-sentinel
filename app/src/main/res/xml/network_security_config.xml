<?xml version="1.0" encoding="utf-8"?>
<!--
  DEV/TESTING ONLY. Permits plain HTTP (cleartext) to the local dev backend
  so the emulator/localhost workflow works without setting up TLS. This must
  be replaced with HTTPS + a proper domain before any real deployment
  (e.g. EC2 with SSL, or a production Wazuh backend) — do not ship this
  cleartext allowance to production.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">54.252.57.123</domain>
    </domain-config>
</network-security-config>
